#include <stdio.h>
int main(){
    int code;
    char symbol;
    printf("input the ASCII code (0 to 127): ");
    scanf("%d",&code);
    symbol=code;
    printf("the symbol corresponding to %d is %c\n",code,symbol);




return 0;
}
